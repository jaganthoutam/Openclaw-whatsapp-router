# TEMPLATE — do NOT commit real secrets.
# Create the secret manually:
#   kubectl create secret generic openclaw-secrets -n openclaw \
#     --from-literal=ADMIN_SECRET=<your-admin-secret> \
#     --from-literal=ROUTER_SECRET=<your-router-secret>
#
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-secrets
  namespace: openclaw
type: Opaque
stringData:
  ADMIN_SECRET: "changeme"       # REPLACE before deploying
  ROUTER_SECRET: "router-secret" # REPLACE before deploying
