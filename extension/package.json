{
  "name": "@openclaw/whatsapp-router",
  "version": "1.0.0",
  "type": "module",
  "openclaw": {
    "extensions": ["./index.ts"],
    "setupEntry": "./setup-entry.ts",
    "channel": {
      "id": "whatsapp-router",
      "label": "WhatsApp Router",
      "blurb": "Connect OpenClaw to WhatsApp via the WhatsApp Router service."
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.9"
  }
}
