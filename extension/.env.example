# Extension Service — copy to .env and fill values

PORT=8090
NODE_ENV=development
LOG_LEVEL=info

# Must match ROUTER_SECRET in the router service
ROUTER_SECRET=router-secret

# Leave empty to use the built-in mock client
OPENCLAW_BASE_URL=
